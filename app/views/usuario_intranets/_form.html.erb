<%= form_with(model: usuario_intranet) do |form| %>
  <% if usuario_intranet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(usuario_intranet.errors.count, "error") %> prohibited this usuario_intranet from being saved:</h2>

      <ul>
        <% usuario_intranet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id_usuario, style: "display: block" %>
    <%= form.number_field :id_usuario %>
  </div>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
